# Необязательные переопределения (по умолчанию значения берутся из ../config.json).
# Создайте файл .env, если нужно переопределить значения локально.

HOST=127.0.0.1
PORT=3001
SQLITE_PATH=./data/dbman.db
